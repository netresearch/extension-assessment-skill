name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@5ef0c079ce82195b2a36a210272d6b661572d83e # v2.14.2
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create extension-assessment skill package
        run: |
          mkdir -p dist-skill
          cp skills/extension-assessment/SKILL.md dist-skill/
          cp LICENSE dist-skill/
          [ -d "skills/extension-assessment/references" ] && cp -r skills/extension-assessment/references dist-skill/
          [ -d "skills/extension-assessment/scripts" ] && cp -r skills/extension-assessment/scripts dist-skill/
          [ -d "skills/extension-assessment/assets" ] && cp -r skills/extension-assessment/assets dist-skill/
          [ -d "skills/extension-assessment/templates" ] && cp -r skills/extension-assessment/templates dist-skill/
          [ -d "skills/extension-assessment/examples" ] && cp -r skills/extension-assessment/examples dist-skill/
          [ -f "skills/extension-assessment/checkpoints.yaml" ] && cp skills/extension-assessment/checkpoints.yaml dist-skill/
          cd dist-skill
          zip -r ../extension-assessment-skill-${{ steps.version.outputs.version }}.zip .
          tar -czvf ../extension-assessment-skill-${{ steps.version.outputs.version }}.tar.gz .

      - name: Create extension-assessment plugin package
        run: |
          mkdir -p dist-plugin
          # Include extension-assessment skill files
          mkdir -p dist-plugin/skills/extension-assessment
          cp skills/extension-assessment/SKILL.md dist-plugin/skills/extension-assessment/
          [ -d "skills/extension-assessment/references" ] && cp -r skills/extension-assessment/references dist-plugin/skills/extension-assessment/
          [ -d "skills/extension-assessment/scripts" ] && cp -r skills/extension-assessment/scripts dist-plugin/skills/extension-assessment/
          [ -d "skills/extension-assessment/assets" ] && cp -r skills/extension-assessment/assets dist-plugin/skills/extension-assessment/
          [ -d "skills/extension-assessment/templates" ] && cp -r skills/extension-assessment/templates dist-plugin/skills/extension-assessment/
          [ -d "skills/extension-assessment/examples" ] && cp -r skills/extension-assessment/examples dist-plugin/skills/extension-assessment/
          [ -f "skills/extension-assessment/checkpoints.yaml" ] && cp skills/extension-assessment/checkpoints.yaml dist-plugin/skills/extension-assessment/
          cp LICENSE dist-plugin/
          # Include plugin manifest
          [ -d ".claude-plugin" ] && cp -r .claude-plugin dist-plugin/
          cd dist-plugin
          zip -r ../extension-assessment-plugin-${{ steps.version.outputs.version }}.zip .
          tar -czvf ../extension-assessment-plugin-${{ steps.version.outputs.version }}.tar.gz .

      - name: Create GitHub Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2.5.0
        with:
          files: |
            extension-assessment-skill-${{ steps.version.outputs.version }}.zip
            extension-assessment-skill-${{ steps.version.outputs.version }}.tar.gz
            extension-assessment-plugin-${{ steps.version.outputs.version }}.zip
            extension-assessment-plugin-${{ steps.version.outputs.version }}.tar.gz
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
